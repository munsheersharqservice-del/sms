# Netlify Configuration for Single Page Application (SPA)
[build]
  command = "vite build"
  publish = "dist"

# Redirect all routes to index.html for React SPA Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
